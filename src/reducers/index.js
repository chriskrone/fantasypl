import { combineReducers } from 'redux';
import defaultReducer from './defaultReducer';

export default combineReducers({
  defaultReducer, // This is a dummy reducer. Remove once an actual reducer is implemented
});
